export const selectParticipants = (state) => state.participants.items;
export const selectIsLoading = (state) => state.participants.isLoading;
export const selectErrorMessage = (state) => state.participants.errorMessage;
